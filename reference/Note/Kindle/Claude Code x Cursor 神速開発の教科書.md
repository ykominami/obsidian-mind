# テスト
## テスト環境の構築
このプロジェクトにJestをインストールして、テスト環境を整えて
## テストケースの生成
calcurator.jsのテストコードを書いて。正常系だけでなく、ゼロ除算や巨大な数値、文字列が入力された場合などの異常系(エッジケース)も網羅して

## 実行と修正のループ
実行する

テストが落ちた。原因を分析して、calculator.jsがワオ修正して、すべてのテストが通るようにして。
# Git管理とドキュメント作成の自動化
## コミットメッセージの自動化
変更内容を要約してコミットメッセージを書く

git diffを見て、変更内容を分かりやすく要約したコミットメッセージを作成して、コミットして
## READMEの自動生成
プロジェクト全体を解析して、README.mdを作成して。概要、インストール方法、使い方の例、ディレクトリ構成図を含めて、初心者でも読んでわかるように書いて

# プロのクオリティに仕上げる
- Cursorので作業も混ぜると早い
- デザインの微調整
	- CSSファイルやTailwindのクラスを直接いじる
- タスク追加モーダル
	- 「タスク追加ボタン」を作り、押したら入力フォームが出てくるようにする
	- 「画面右下にフローティングアクションボタン(+)を追加して、押すとタスク追加のモーダルが開くようにして」
- ローディング表示
	- データ取得中にグルグル回るスピナーを追加します
	- 「@App.tsxデータ取得中はローディングスピナーを表示して」

![[Pasted image 20260505160647.png]]