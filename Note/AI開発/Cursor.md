# Cursor
## 「売上データCSVをExcelに取り込む」「請求書を作成する」の2つに分ける
## 壁打ち
- @csv/ のCSVファイルを読み取って 売上.xlsx の売りあてシートに出力したい。読取り済のCSVファイルはCSVフォルダの中に「読取済」フォルダを作って移動。Pythonで作成する。
- 上記をPlanモードで壁打ち
## マーメイド図で視覚化
- @todo.md の仕様でマーメイド図を作成して。
-  |```mermaid
 | ```
## プロジェクトルールの作成
- Python実行環境は.venvで管理する。
## Agentモードで実装
- @todo.md にしたがってpythonプログラムを作成して。
## Smithery
https://smithery.ai

# 「請求書発行プログラムの仕様書作成と実装」
- プロンプト
```
こらから以下の仕様に基づく仕様書を作成したい。まずは、excep mcpで請求書.xlsx と 売上.xlsx １の構造（シート名、使用中のセル、入力されている数式）を調べて。

以下の処理を行う Python プログラムを作成したい。OSはWindows。以下の内容で仕様書 bill_todo.md を作成して。使用するライブラリ、技術スタックも記載すること。

1.任意の年月を市営
2.売り上げ.xlsx の「売上」シートから該当年月のデータを抽出
 1.売上日はA列
 2.商品名はG列
 3.数量はM列
 4.単価はP列
3.請求書.xlsx の「請求書（原紙）シートをコピーして取引先ごとの請求書シートを作成
4.取引先ごとの請求書シート名の形式は売上.xlsx の「取引先」シート名の形式は、「請求先名_YYMM」とする
5.請求書に記載する郵便番号、住所、得意先名は売上.xlsx の「取引先」シートにあるいかのデータを利用
 1.A列の得意先CDと「売上」シートのK列の取引先CDで照合
 2.郵便番号はC列、請求書出力先はA4。
 3.住所はD列、請求書出力先はA5。
 4.得意先はB列。請求書出力先はA6.
5.取引データは20行目以降に出力する。
 1.日付はA列
 2.商品名はB列
 3.数量はF列
 4.単価はG列
7.原紙は35行目までしかないのでそれ以上のデータは行を挿入して追記
8.すべての取引先についてシートを作成したらシート名と同名のPDFを出力する
9.PDFの出力先は売上.xlsx と同じ階層にPDFファイルだを作成して出力する
10.PDF出力が終わったら各取引先ごとのシートは削除する
```
対象年月(YYMM または YYYY-MMなど)の指定方法をGUI,ライブラリは標準のもの
## 実装
@bill_todo.mdにしたがって請求書発行Pythonプログラムを実装して

# なかなｋうまくいかない場合
1.@シンボルでより有用なコンテキストを与える
 - Cursorに組み込まれている公式ドキュメントへの参照
 - ユーザーが公式ドキュメントへのリンクをURLで与える
 - AIにWEB検索させて最新の情報を見つけてコンテキストとして追加
 - @入力したときに表示されるリストの中で、Docsを矢印で選択し、Tabキーを押す
 - Add new doc
- ユーザが公式ドキュメントへのリンクをユーザに与える
- AIにWeb検索させて最新の情報を見つけてコンテキストとして追加
2.より高度なモデルに変更
3.より詳細なプロンプトを入力
4.最終手段として仕様書を削除し、一から作り直し
5.タスクを分割する

# デスクトップアプリ化の指示
- [OS名] 環境で配布できるように @csv_import.py と@bill_generator.py をそれぞれデスクトップアプリ化して
# 一連のデスクトップ化作業をスクリプト化する
- 一連のデスクトップアプリ化のビルド作業をスクリプトにして
# サクッとプログラムを実行する方法
- プログラムがあるところへ移動して、じっこうするスクリプトを作成
- @bill_generator.py をダブルクリックで実行できるスクリプトを実装して
- 相対パスのまま移動できるようにする。ダブルクリック用スクリプトを右クリックし、Windowsの場合はショートカット作成、macOSの場合はエイリアスを作成して、任意の場所にショートカットおよびエイリアスを移動。
# README.mdの作成
- @csv_import, @bill_generator のreadme.mdを作成して